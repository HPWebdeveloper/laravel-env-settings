# how to try it

Run the bootstrap as usual:

```bash
composer install
chmod +x bin/bootstrap
bin/bootstrap
```

Accept or enter versions; choose whether to align composer constraints

After it runs, check:

static-analysis.yml shows php-version: "<latest selected>"
